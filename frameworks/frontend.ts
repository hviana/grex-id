// Frontend component registry — aggregated across all frameworks.
// This file is NOT server-only so component registrations are
// available in the client bundle.

export function registerAllFrameworksFrontend(): void {
  // Future frameworks register here:
  // registerFooFrameworkFrontend();
}
