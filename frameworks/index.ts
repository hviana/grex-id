// Framework boot entry point (§26.4).
// Each framework registers via its own index.ts.
// This file aggregates all framework registrations.
// Subsystems (systems) register via systems/index.ts — not here.

export function registerAllFrameworks(): void {
  // Future frameworks register here:
  // registerFooFramework();
}
